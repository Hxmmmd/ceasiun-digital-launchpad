import { createFileRoute,Link } from "@tanstack/react-router";import { useEffect,useState } from "react";import { ArrowUpRight } from "lucide-react";import { supabase } from "@/integrations/supabase/client";import { Layout,PageIntro,meta } from "@/components/site";export const Route=createFileRoute("/careers")({head:()=>meta("Careers","Explore career opportunities and working culture at Ceasiun."),component:Page});function Page(){const [jobs,setJobs]=useState<any[]>([]);useEffect(()=>{supabase.from("career_openings").select("*").eq("is_visible",true).then(({data})=>setJobs(data??[]))},[]);return <Layout><PageIntro eyebrow="Careers" title="Do serious work with people who care about the details." copy="We value clear thinking, ownership, honest communication, and the discipline to keep improving."/><section className="section shell"><div className="values-grid">{["Own the outcome","Stay curious","Communicate clearly","Make it maintainable"].map(x=><article key={x}><h2>{x}</h2><p>Our culture rewards practical judgement and work that earns trust over time.</p></article>)}</div><div className="jobs"><p className="eyebrow">Open roles</p>{jobs.length?jobs.map(j=><article key={j.id}><div><h2>{j.title}</h2><span>{j.location} · {j.type}</span></div><p>{j.description}</p><Link to="/contact" search={{service:""}}>Apply <ArrowUpRight/></Link></article>):<div className="empty-proof"><h3>No open roles right now.</h3><p>Follow Ceasiun on LinkedIn for future opportunities.</p></div>}</div></section></Layout>}
