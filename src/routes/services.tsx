import { createFileRoute,Link } from "@tanstack/react-router";import { ArrowUpRight,Check } from "lucide-react";import { Layout,PageIntro,CTA,meta } from "@/components/site";import { services,samples } from "@/lib/site-data";export const Route=createFileRoute("/services")({head:()=>meta("Digital services","Explore Ceasiun’s nine connected service areas, from engineering and growth to automation, security, and managed operations."),component:Page});function Page(){return <Layout><PageIntro eyebrow="Services" title="Capability without fragmentation." copy="Nine specialist practices, connected around the outcomes your business needs."/><section className="section shell service-list">{services.map((s,i)=><article id={s.slug} key={s.slug}><div><span>0{i+1}</span><s.icon/><h2>{s.title}</h2><p>{s.short}</p></div><ul>{s.items.map(x=><li key={x}><Check/>{x}</li>)}</ul><Link to="/contact" search={{service:s.title}}>Discuss this service <ArrowUpRight/></Link></article>)}</section><section className="section shell"><h2>Related work structures</h2><div className="mini-grid">{samples.map(s=><Link key={s.slug} to="/work/$slug" params={{slug:s.slug}}><span>{s.category}</span><h3>{s.title}</h3></Link>)}</div></section><CTA/></Layout>}
